spring:
  application:
    name: content-service
  cloud:
    nacos:
      server-addr: 192.168.12.128:8848
      discovery:
        namespace: dev_study
        group: study
      config:
        namespace: dev_study
        group: study
        file-extension: yaml
        refresh-enabled: true
      username: nacos
      password: nacos
  #profiles默认为dev
  profiles:
    active: dev
